language: python

python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "nightly"

sudo: required

dist: trusty

addons:
    apt:
        packages:
            - python-setuptools
            - python-magic

install:
    - sudo make install

script:
    - make test
